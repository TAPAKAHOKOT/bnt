services:
  backend:
    build: .
    container_name: bnt-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    # 512 MB keeps NumPy/audio spikes from getting OOM-killed.
    mem_limit: 512m
